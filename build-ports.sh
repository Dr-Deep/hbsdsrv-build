#!/bin/sh -eux

# overlays?
# poudriere cmd args?
# welcher jail?
# port tree
# poudriere set
# pkglist / -a

# doas poudriere ports -u -p "${PORTS}"
# doas poudriere bulk ${POUDRIERE_ARGS}  -j "${JAIL}" -p "${PORTS}" -z "${SET}" -f "${PKGLIST}" 

